
include "application.conf"

db {
  default.driver = org.postgresql.Driver
  default.url = "jdbc:postgresql://localhost:5432"
  default.username = postgres
  default.password = "root"
  default.dbName = "translator_test"
}
